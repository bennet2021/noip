#include <bits/stdc++.h>
using namespace std;
int main(int argc, char** argv) {
	int a;
	cin>>a;
	if(a==1 || a==0)
	{
		cout<<"Today, I ate "<<a<<" apple."<<endl;
	}
	if(a>=2)
	{
		cout<<"Today, I ate "<<a<<" apples."<<endl;
	}
	return 0;
}
